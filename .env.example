# Required — get from https://discord.com/developers/applications
DISCORD_TOKEN=your_bot_token_here
CLIENT_ID=your_client_id_here

# Optional — set for instant command deploy (dev). Omit for global deploy (takes ~1hr).
GUILD_ID=
